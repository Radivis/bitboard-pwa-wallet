/** ~10% smaller amount text on lab block/tx cards so the network unit prefix fits without wrapping. */
export const LAB_CARD_TX_AMOUNT_TEXT_CLASS = 'text-[0.8rem] leading-tight'

/** Fees row on block squares. Smaller size prevents text wrapping and indicates lesser importance than moved amount.*/
export const LAB_CARD_BLOCK_FEE_AMOUNT_TEXT_CLASS = 'text-[0.75rem] leading-tight'

/** Net moved row on block squares. Small size prevents text wrapping.*/
export const LAB_CARD_BLOCK_NET_MOVED_AMOUNT_TEXT_CLASS = 'text-[0.8rem] leading-tight'
