/**
 * Basenames for SQLite files stored in OPFS via `WaSqliteWorkerDialect` (`preferOPFS: true`).
 * Keep in sync with exports and dialect configuration.
 */
export const WALLET_SQLITE_OPFS_BASENAME = 'bitboard-wallet'
export const LAB_SQLITE_OPFS_BASENAME = 'bitboard-lab'
