[workspace.package]
version = "0.1.0-alpha"

[workspace]
members = ["crypto", "bitboard-encryption", "bitboard-lightning"]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
